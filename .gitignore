# CI builds regenerate these; WAVs are too large for the repo (DEVELOPMENT-PLAN.md §Phase 5).
app/src/main/res/raw/*.wav

# Keystores & secrets must never enter VCS.
*.jks
*.keystore
keystore.properties
.env

# Local SDK paths & IDE
local.properties
.idea/
.gradle/
build/
**/build/
.cxx/
*.iml

# OS noise
.DS_Store
